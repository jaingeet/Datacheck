
<%= link_to("<< back to List", {:action => 'index'}, :class =>'back-link') %>
<div class="employee delete">
<h2> Delete Employee </h2>
<%= form_for @employee, url: {action: 'destroy', id: @employee.id}, method: :delete, html: {class: "employee_registration"} do |f| -%>
  <p> Are you sure you want to permanently delete this employee ? </p>
  <p> <%= @employee.id %> </p>
<%= f.submit "Detele" %>
<% end -%>
</div>